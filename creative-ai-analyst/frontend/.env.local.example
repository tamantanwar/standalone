# Copy to .env.local for local dev
# These are read by Next.js at runtime — never commit a real .env.local

# Backend URL — set automatically by docker-compose
BACKEND_URL=http://localhost:8000

# Public env vars (exposed to browser — only put non-secret values here)
NEXT_PUBLIC_APP_NAME="Data AI Analyst"
